import { FormEvent, useEffect, useState } from 'react';
import { archiveGoal, createGoal, GoalSummary, listGoals, releaseGoal, reserveGoal, Wallet } from './lib/api';
const money=(n:number,c:string)=>new Intl.NumberFormat(undefined,{style:'currency',currency:c,maximumFractionDigits:0}).format(n);
export function GoalPanel({householdId,wallets}:{householdId:string;wallets:Wallet[]}){const[goals,setGoals]=useState<GoalSummary[]>([]);const[error,setError]=useState('');const[name,setName]=useState('');const[currency,setCurrency]=useState(wallets[0]?.currency||'IDR');const[target,setTarget]=useState('');const refresh=()=>listGoals(householdId).then(setGoals).catch(e=>setError(String(e.message||e)));useEffect(()=>{void refresh()},[householdId]);async function add(e:FormEvent){e.preventDefault();setError('');try{await createGoal(householdId,{name,currency,targetAmountMinor:Number(target)});setName('');setTarget('');await refresh()}catch(e){setError(e instanceof Error?e.message:'goal_error')}}return <section className="panel" aria-label="Financial goals"><div className="section-heading"><div><p className="eyebrow">Reserved savings</p><h2>Financial goals</h2><p>Goal progress is backed by money actually reserved from a wallet. Reserved money remains yours, but is no longer ordinary available-to-spend money.</p></div></div><form className="inline-form" onSubmit={add}><input aria-label="Goal name" placeholder="Goal name" value={name} onChange={e=>setName(e.target.value)} required/><input aria-label="Goal target" type="number" min="1" placeholder="Target (minor units)" value={target} onChange={e=>setTarget(e.target.value)} required/><select aria-label="Goal currency" value={currency} onChange={e=>setCurrency(e.target.value)}>{[...new Set(wallets.map(w=>w.currency))].map(c=><option key={c}>{c}</option>)}</select><button type="submit">Create goal</button></form>{error&&<p role="alert">{error}</p>}<div className="stack">{goals.map(g=><GoalCard key={g.id} goal={g} wallets={wallets.filter(w=>w.status==='active'&&w.currency===g.currency)} householdId={householdId} refresh={refresh}/>)}</div></section>}
function GoalCard({goal,wallets,householdId,refresh}:{goal:GoalSummary;wallets:Wallet[];householdId:string;refresh:()=>void}){const[walletId,setWalletId]=useState(wallets[0]?.id||'');const[amount,setAmount]=useState('');const[error,setError]=useState('');const act=async(kind:'reserve'|'release')=>{setError('');try{const input={walletId,amountMinor:Number(amount)};if(kind==='reserve')await reserveGoal(householdId,goal.id,input);else await releaseGoal(householdId,goal.id,input);setAmount('');refresh()}catch(e){setError(e instanceof Error?e.message:'goal_error')}};const pct=goal.targetAmountMinor?Math.min(100,Math.round(goal.reservedMinor/goal.targetAmountMinor*100)):0;return <article className="budget-card"><div className="budget-card__header"><strong>{goal.name}</strong><span>{goal.status}</span></div><p>{money(goal.reservedMinor,goal.currency)} reserved of {money(goal.targetAmountMinor,goal.currency)} · {pct}%</p><p><strong>{money(goal.remainingMinor,goal.currency)}</strong> remaining</p><progress max={goal.targetAmountMinor} value={Math.min(goal.reservedMinor,goal.targetAmountMinor)}/>{goal.status==='active'&&<><div className="inline-form"><select aria-label={`Wallet for ${goal.name}`} value={walletId} onChange={e=>setWalletId(e.target.value)}>{wallets.map(w=><option key={w.id} value={w.id}>{w.name}</option>)}</select><input aria-label={`Amount for ${goal.name}`} type="number" min="1" value={amount} onChange={e=>setAmount(e.target.value)} placeholder="Amount"/><button type="button" onClick={()=>void act('reserve')} disabled={!walletId}>Reserve</button><button type="button" onClick={()=>void act('release')} disabled={!walletId}>Release</button><button type="button" onClick={()=>void archiveGoal(householdId,goal.id).then(refresh)}>Archive</button></div>{error&&<p role="alert">{error}</p>}</>}</article>}
