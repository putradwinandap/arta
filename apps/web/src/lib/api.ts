export type Household = { id: string; name: string }; export type HouseholdMembership = Household & { role: string };
export type WalletType = 'cash' | 'bank' | 'e_wallet' | 'other'; export type WalletStatus='active'|'archived'; export type TransactionKind='income'|'expense'; export type CaptureStatus='pending'|'confirmed'; export type GoalStatus='active'|'archived';
export type Wallet={id:string;householdId:string;name:string;type:WalletType;currency:string;status:WalletStatus;archivedAt?:string}; export type WalletBalance={walletId:string;amountMinor:number;reservedMinor?:number;availableMinor?:number;currency:string}; export type Activity={id:string;type:TransactionKind|'transfer';walletId?:string;sourceWalletId?:string;destinationWalletId?:string;amountMinor:number;currency:string;occurredAt:string;note?:string}; export type TransactionCapture={id:string;householdId:string;walletId?:string;kind?:TransactionKind;amountMinor:number;note?:string;source:'quick_manual';status:CaptureStatus;capturedAt:string;updatedAt:string;confirmedAt?:string;confirmedTransactionId?:string}; export type BudgetSummary={id:string;householdId:string;currency:string;periodStart:string;periodEnd:string;amountMinor:number;spentMinor:number;remainingMinor:number}; export type GoalSummary={id:string;householdId:string;name:string;currency:string;targetAmountMinor:number;status:GoalStatus;archivedAt?:string;reservedMinor:number;remainingMinor:number}; export type FinanceOverview={balances:WalletBalance[];totals:{incomeMinor:number;expenseMinor:number};activity:Activity[];currentBudgets?:BudgetSummary[];activeGoals?:GoalSummary[];recentActivity?:Activity[]}; export type Reconciliation={id:string;householdId:string;walletId:string;currency:string;expectedAmountMinor:number;observedAmountMinor:number;discrepancyMinor:number;status:'unresolved'|'reconciled';adjustmentId?:string;observedAt:string;resolvedAt?:string};
type ApiErrorBody={error?:string}; async function request<T>(path:string,init?:RequestInit):Promise<T>{const response=await fetch(path,{...init,credentials:'same-origin',headers:{'Content-Type':'application/json',...init?.headers}});if(!response.ok){let code=`http_${response.status}`;try{const body=(await response.json())as ApiErrorBody;code=body.error||code}catch{}throw new Error(code)}if(response.status===204)return undefined as T;return response.json()as Promise<T>}
export async function listMyHouseholds(){return(await request<{households:HouseholdMembership[]}>('/api/auth/households')).households}
export function createHousehold(name:string){return request<Household>('/api/households/',{method:'POST',body:JSON.stringify({name})})} export function getHousehold(h:string){return request<Household>(`/api/households/${h}`)} export async function listWallets(h:string){return(await request<{wallets:Wallet[]}>(`/api/households/${h}/wallets/`)).wallets} export function createWallet(h:string,input:{name:string;type:WalletType;currency:string}){return request<Wallet>(`/api/households/${h}/wallets/`,{method:'POST',body:JSON.stringify(input)})} export function updateWallet(h:string,w:string,input:{name:string;type:WalletType}){return request<Wallet>(`/api/households/${h}/wallets/${w}`,{method:'PATCH',body:JSON.stringify(input)})} export function archiveWallet(h:string,w:string){return request<Wallet>(`/api/households/${h}/wallets/${w}/archive`,{method:'POST'})}
export function createTransaction(h:string,input:{walletId:string;kind:TransactionKind;amountMinor:number;note?:string}){return request(`/api/households/${h}/transactions`,{method:'POST',body:JSON.stringify(input)})} export function createTransfer(h:string,input:{sourceWalletId:string;destinationWalletId:string;amountMinor:number;note?:string}){return request(`/api/households/${h}/transfers`,{method:'POST',body:JSON.stringify(input)})} export function getFinanceOverview(h:string){return request<FinanceOverview>(`/api/households/${h}/finance`)}
export function createCapture(h:string,input:{id:string;amountMinor:number;note?:string;capturedAt?:string}){return request<TransactionCapture>(`/api/households/${h}/captures/`,{method:'POST',body:JSON.stringify(input)})} export async function listPendingCaptures(h:string){return(await request<{captures:TransactionCapture[]}>(`/api/households/${h}/captures/`)).captures} export function reviewCapture(h:string,c:string,input:{walletId:string;kind:TransactionKind;amountMinor:number;note?:string}){return request<TransactionCapture>(`/api/households/${h}/captures/${c}`,{method:'PATCH',body:JSON.stringify(input)})} export function confirmCapture(h:string,c:string){return request(`/api/households/${h}/captures/${c}/confirm`,{method:'POST'})}
export function createBudget(h:string,input:{currency:string;periodStart:string;periodEnd:string;amountMinor:number}){return request<BudgetSummary>(`/api/households/${h}/budgets/`,{method:'POST',body:JSON.stringify(input)})} export async function listBudgets(h:string){return(await request<{budgets:BudgetSummary[]}>(`/api/households/${h}/budgets/`)).budgets} export function getBudget(h:string,b:string){return request<BudgetSummary>(`/api/households/${h}/budgets/${b}`)}
export function createGoal(h:string,input:{name:string;currency:string;targetAmountMinor:number}){return request<GoalSummary>(`/api/households/${h}/goals/`,{method:'POST',body:JSON.stringify(input)})} export async function listGoals(h:string){return(await request<{goals:GoalSummary[]}>(`/api/households/${h}/goals/`)).goals} export function updateGoal(h:string,g:string,input:{name:string;targetAmountMinor:number}){return request<GoalSummary>(`/api/households/${h}/goals/${g}`,{method:'PATCH',body:JSON.stringify(input)})} export function archiveGoal(h:string,g:string){return request<GoalSummary>(`/api/households/${h}/goals/${g}/archive`,{method:'POST'})} export function reserveGoal(h:string,g:string,input:{walletId:string;amountMinor:number}){return request<GoalSummary>(`/api/households/${h}/goals/${g}/reserve`,{method:'POST',body:JSON.stringify(input)})} export function releaseGoal(h:string,g:string,input:{walletId:string;amountMinor:number}){return request<GoalSummary>(`/api/households/${h}/goals/${g}/release`,{method:'POST',body:JSON.stringify(input)})}
export function createReconciliation(h:string,w:string,input:{observedAmountMinor:number}){return request<Reconciliation>(`/api/households/${h}/wallets/${w}/reconcile`,{method:'POST',body:JSON.stringify(input)})} export async function listReconciliations(h:string){return(await request<{reconciliations:Reconciliation[]}>(`/api/households/${h}/reconciliations/`)).reconciliations} export function adjustReconciliation(h:string,r:string,input:{reason:string}){return request<Reconciliation>(`/api/households/${h}/reconciliations/${r}/adjust`,{method:'POST',body:JSON.stringify(input)})}