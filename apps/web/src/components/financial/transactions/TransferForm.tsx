export function TransferForm(p:any){return <section className="panel transfer-panel"><p className="eyebrow">Move money</p><h2>Wallet transfer</h2>{p.activeWallets.length<2?<div className="empty-state"><p>Add another active wallet to transfer money.</p></div>:<form className="stack-form" onSubmit={p.handleTransfer}>{[["source","From","transferSourceId","setTransferSourceId"],["destination","To","transferDestinationId","setTransferDestinationId"]].map((x:any)=><label key={x[0]}>{x[1]}<select aria-label={`Transfer ${x[0]}`} value={p[x[2]]} onChange={(e:any)=>p[x[3]](e.target.value)}>{p.activeWallets.map((w:any)=><option key={w.id} value={w.id}>{w.name}</option>)}</select></label>)}<label>Amount<input aria-label="Transfer amount" type="number" min="1" step="1" value={p.transferAmount} onChange={(e:any)=>p.setTransferAmount(e.target.value)} required/></label><label>Note<input aria-label="Transfer note" value={p.transferNote} onChange={(e:any)=>p.setTransferNote(e.target.value)} placeholder="Move to savings"/></label><button disabled={p.saving}>Transfer money</button></form>}</section>}
